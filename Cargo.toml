[package]
name = "fvd_portfolio"
version = "0.1.0"
edition = "2021"

[dependencies]
rocket = { version = "0.5.0", features = ["tls"] }
rocket_async_compression = "0.5.0"
itertools = "0.12.0"

[dev-dependencies]
curl = "0.4.44"
